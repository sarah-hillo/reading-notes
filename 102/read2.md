**using command lines we can clone files 
By cloning the file, you have copied all versions of all files for a project. 
and then we can check files,track and stage files,commit a file and pushing all changes.**

**Local Repository Structure**
The local Git repository has three components:

- **Working Directory:** The actual files reside here.
- **Index:** The area used for staging
- **Head:** Points to the most recent commit
